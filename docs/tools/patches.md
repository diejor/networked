# Moved

The free-form patch notes that lived here have been replaced by real unified-diff
patches in [`patches/`](patches/), applied to a pristine vendored upstream copy
of `make_rst.py` (see [`vendor/UPSTREAM.md`](vendor/UPSTREAM.md)) by
[`build_make_rst.py`](build_make_rst.py).

See [`patches/README.md`](patches/README.md) for the patch index and the editing
workflow.
