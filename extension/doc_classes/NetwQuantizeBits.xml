<?xml version="1.0" encoding="UTF-8" ?>
<class name="NetwQuantizeBits" inherits="NetwQuantize" api_type="extension" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/godotengine/godot/master/doc/class.xsd">
	<brief_description>
		[NetwQuantize] that maps a bounded range onto an exact bit count per component.
	</brief_description>
	<description>
		A value in [member min_limit]..[member max_limit] is linearly mapped to [member bit_count] bits. [Vector3] or [Vector2] quantizes each axis with the same range. Best for normalized directions and bounded scalars (e.g. [code]motion[/code] with [code]bit_count = 8, [-1, 1][/code]).
		The grid spans the declared range inclusively and holds an odd number of levels, so three values a game depends on all survive the wire: both declared limits, and the rest point of a symmetric range.
		Those three cannot coexist on a [code]2^bit_count[/code] grid. Spanning [member min_limit] to [member max_limit] inclusively over [code]N[/code] levels puts the midpoint on a level only when [code]N[/code] is odd, and [code]2^bit_count[/code] is even. The grid therefore uses [code]2^bit_count - 1[/code] levels and leaves one bit pattern unused, which costs under one percent of resolution at eight bits and nothing measurable above sixteen.
		[codeblock]
		var q := NetwQuantizeBits.new().bits(8).limits(-1.0, 1.0)

		bits(4).limits(-1, 1)   15 levels, step 2/14
		  -1.0  ──&gt; -1.0    the declared floor
		   0.0  ──&gt;  0.0    a stick at neutral, a body at rest
		  +1.0  ──&gt; +1.0    the declared ceiling
		[/codeblock]
		A quantizer whose endpoints do not round-trip is invisible to every comparison, because both peers encode the same code, while the two games read values a whole step apart. Nothing downstream can attribute that, which is why exactness here is a wire property rather than a tuning preference.
	</description>
	<tutorials>
	</tutorials>
	<methods>
		<method name="bits">
			<return type="NetwQuantizeBits" />
			<param index="0" name="bits" type="int" />
			<description>
				Builder that sets [member bit_count] and returns this quantizer.
			</description>
		</method>
		<method name="limits">
			<return type="NetwQuantizeBits" />
			<param index="0" name="min" type="float" />
			<param index="1" name="max" type="float" />
			<description>
				Builder that sets [member min_limit] and [member max_limit] and returns this quantizer.
			</description>
		</method>
	</methods>
	<members>
		<member name="bit_count" type="int" setter="set_bit_count" getter="get_bit_count" default="8">
			Bits per component, clamped to 1..32.
		</member>
		<member name="max_limit" type="float" setter="set_max_limit" getter="get_max_limit" default="1.0">
			Inclusive upper bound of the encoded range.
		</member>
		<member name="min_limit" type="float" setter="set_min_limit" getter="get_min_limit" default="-1.0">
			Inclusive lower bound of the encoded range.
		</member>
	</members>
</class>
