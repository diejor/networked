<?xml version="1.0" encoding="UTF-8" ?>
<class name="NetwJoinConfig" inherits="RefCounted" api_type="extension" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/godotengine/godot/master/doc/class.xsd">
	<brief_description>
		Everything one declaration says about the wire schema of its join handler.
	</brief_description>
	<description>
		[method Netw.configure_join] declares the handler run once for each join its session accepts, and answers this config, which carries the packing that one declaration asks for. It belongs to that declaration: a second node declaring the same handler gets its own, and re-declaring on one node replaces it.
		A client fills only the handler's parameters after the [NetwParticipant] the server supplies itself, and passes them as the [code]args[/code] of [method Netw.join]. So [member quantizers] lines up with that shorter list, and its position zero is the handler's second parameter.
		An author who declared nothing carries an empty [member quantizers], which sends every wire argument self-describing. A null slot inside a declared list does the same for that one argument, so a schema packs the fields worth packing and leaves the rest alone.
		[codeblock]
		func _init() -&gt; void:
		    Netw.configure_join(self, spawn_at) \
		            .quantize(null, NetwQuantizeScalar.new().bits(4).limits(0, 8))

		func spawn_at(who: NetwParticipant, point: StringName, team: int) -&gt; void:
		    ...
		[/codeblock]
		Whether a declared quantizer can pack the parameter it sits over is judged against the handler method's own declared argument types, past the [NetwParticipant] the client never sends. The rule applied to that list is the same one [NetwMemberConfig] enforces for a script member, spelled once. A mismatch is reported and the declaration is still stored, which keeps a mispacked schema visible in the editor without changing what a release build sends.
	</description>
	<tutorials>
	</tutorials>
	<methods>
		<method name="quantize" qualifiers="vararg">
			<return type="NetwJoinConfig" />
			<description>
				Declares [member quantizers], one [NetwQuantize] per argument in the order the handler declares them past its [NetwParticipant], and [code]null[/code] for an argument that travels self-describing. The count is checked against that sliced schema, so a handler taking two wire arguments and given one quantizer is refused rather than packed halfway, and a handler whose parameters cannot be read at all is taken on trust. Passing nothing, or an argument that is not a [NetwQuantize], is refused with an error and leaves the previous declaration standing, because a typo in one chained call should not silently unpack a schema that was already declared.
				Answers the same config so the declaration chains.
			</description>
		</method>
	</methods>
</class>
