version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
  commands:
    - pip install -r docs/requirements.txt
    - cd docs && python -m sphinx -T --keep-going -b html -d _build/doctrees . $READTHEDOCS_OUTPUT/html

